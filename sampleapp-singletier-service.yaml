apiVersion: v1
kind: Service
metadata:
  name: sampleapp-onetier-service
spec:
  type: NodePort
  ports:
  - port: 8088
    nodePort: 30088
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: sampleapp-onetier
